<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="top"
    android:paddingVertical="6dp">

    <ImageView
        android:id="@+id/ivBugAvatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_mascot"
        android:contentDescription="Dr.Bug" />

    <!-- Cột dọc: bong bóng text + vùng action (quest cards / nút redirect) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="40dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_chat_bubble_ai"
            android:paddingHorizontal="16dp"
            android:paddingVertical="12dp"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:lineSpacingExtra="2dp" />

        <!-- Quest cards: RecyclerView ngang, chỉ hiện khi action_type=show_quests -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerQuests"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:clipToPadding="false"
            android:visibility="gone" />

        <!-- Placeholder khi quest đang cập nhật (Quest Engine chưa build) -->
        <TextView
            android:id="@+id/tvQuestPlaceholder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_chat_bubble_ai"
            android:paddingHorizontal="14dp"
            android:paddingVertical="10dp"
            android:text="Các quest gợi ý đang được cập nhật."
            android:textColor="@color/text_hint"
            android:textSize="13sp"
            android:textStyle="italic"
            android:visibility="gone" />

        <!-- Nút redirect: chỉ hiện khi action_type=redirect_feature -->
        <TextView
            android:id="@+id/btnRedirect"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_chat_pill"
            android:paddingHorizontal="20dp"
            android:paddingVertical="12dp"
            android:textColor="@color/cyan_glow"
            android:textSize="14sp"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground"
            android:visibility="gone" />
    </LinearLayout>

</LinearLayout>
